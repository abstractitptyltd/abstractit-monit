check device <%= @name %> with path <%= @real_path %>
  if space usage > <%= @disk_usage %> for <%= @checks %> times within <%= @cycles %> cycles then alert
  if inodes > <%= @inode_usage %> for <%= @checks %> times within <%= @cycles %> cycles then alert
